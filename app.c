// População, área, PIB, Num. de pontos turísticos 
// Estado (char), Cód. carta (char[]), Nome da cidade (char[]), População (int), Área km2 (float), PIB (float), Pontos turistícos (Int)

#include <stdio.h>
#include <locale.h>
#include <string.h>

int main()
{
    setlocale(LC_ALL, "Portuguese_Brazil");
    printf("Olá, mundo!");
    return 0;
}